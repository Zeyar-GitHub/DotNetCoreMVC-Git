@model IEnumerable<DotNetCoreMVC.Models.EmployeeModel.Employee>

@{
    ViewData["Title"] = "Employee List";
}

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1 class="mb-4">Welcome, @ViewBag.Username!</h1>
            <h2 class="text-center mb-4">Employee List</h2>
            <div class="mt-4">
                <partial name="_EmployeeList" />
            </div>
        </div>
    </div>
</div>
